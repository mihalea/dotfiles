#!/bin/zsh

alias ls='ls --color=auto'
alias l=ls
alias ll='ls -l'
alias la='ls -la'
alias sudo='sudo '
alias update-grub="grub-mkconfig -o /boot/grub/grub.cfg"
alias batch-yt="youtube-dl -x --audio-format mp3 -o '%(title)s.%(ext)s' -a "
